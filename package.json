{
  "name": "hexpiano",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check src/app.js && node --check src/engine.js && node --check src/tuning.js",
    "test": "node --test",
    "verify": "npm run check && npm test",
    "serve": "python3 -m http.server 4177"
  }
}
